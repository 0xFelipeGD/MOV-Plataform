# ==============================================================================
# MOV Platform - Configuração de Variáveis de Ambiente
# ==============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Substitua os valores de exemplo por valores seguros
# 3. Para gerar credenciais seguras automaticamente: ./scripts/generate_credentials.sh > .env
# ==============================================================================

# ------------------------------------------------------------------------------
# MQTT Broker (Mosquitto)
# ------------------------------------------------------------------------------
MQTT_USER=mov_mqtt_user
MQTT_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_MQTT

# ------------------------------------------------------------------------------
# Banco de Dados (InfluxDB)
# ------------------------------------------------------------------------------
INFLUX_USER=mov_admin
INFLUX_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_INFLUX
INFLUX_ORG=MOV
INFLUX_BUCKET=sensores
INFLUX_TOKEN=SUBSTITUA_POR_TOKEN_SEGURO_256_BITS

# ------------------------------------------------------------------------------
# Visualização (Grafana)
# ------------------------------------------------------------------------------
GRAFANA_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_GRAFANA

# ------------------------------------------------------------------------------
# Analytics (Python) - Configurações Opcionais
# ------------------------------------------------------------------------------
# Threshold de temperatura para alertas (padrão: 30.0)
ANALYTICS_TEMP_THRESHOLD=30.0
